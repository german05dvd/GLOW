// HeroScene is intentionally disabled.
// Kept as a stub to avoid module-not-found build errors
// while the hero visual is handled entirely in Hero.tsx.
export function HeroScene() {
  return null;
}